apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foodcoreapi.fullname" . }}-test-connection"
  labels:
    {{- include "foodcoreapi.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "foodcoreapi.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
