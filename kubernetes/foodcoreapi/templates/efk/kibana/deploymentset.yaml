apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "kibana.fullname" . }}-deployment
  labels:
    app: {{ include "kibana.name" . }}
spec:
  replicas: {{ .Values.kibana.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "kibana.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "kibana.name" . }}
    spec:
      containers:
        - name: {{ include "kibana.name" . }}
          image: "{{ .Values.kibana.image.repository }}:{{ .Values.kibana.image.tag }}"
          ports:
            - containerPort: {{ .Values.kibana.ports.port }}
              name: {{ .Values.kibana.ports.name }}
          volumeMounts:
            - name: {{ .Values.kibana.volumes.kibana-config.name }}
              mountPath: {{ .Values.kibana.volumes.kibana-config.mountPath }}
              subPath: {{ .Values.kibana.volumes.kibana-config.subPath }}
          resources:
            limits:
              cpu: {{ .Values.kibana.resources.limits.cpu }}
            requests:
              cpu: {{ .Values.kibana.resources.requests.cpu }}
      volumes:
        - name: {{ .Values.kibana.volumes.kibana-config.name }}
          configMap:
            name: {{ include "kibana.fullname" . }}-config